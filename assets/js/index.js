'use strict';

/**************************************************
slick
**************************************************/
$(document).ready(function() {
    $('.slider').slick({
        // setting-name:setting-value
        autoplay: true,
        
    });
});